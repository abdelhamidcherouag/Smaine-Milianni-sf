{% extends 'base.html.twig' %}



{% block body %}

    <div class="container">
        <div class="section">
            <div class="row">
                <div class="col s12 m12">
                    <h1>Modifier de voiture {{ Car.model }}</h1>
                    {% include 'home/form.html.twig' %}
                    <button class="btn waves-effect waves-light" type="submit" name="action">Submit
                        <i class="material-icons right">Modifier</i>
                    </button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>


{% endblock %}
